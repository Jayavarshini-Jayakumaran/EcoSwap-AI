# ── Gemini API key ────────────────────────────
# Key lives in Cloudflare Workers secrets, NOT here
.env
.env.local
.env.*.local

# Old client-side key config (no longer used)
js/config.js

# ── Cloudflare / Wrangler ─────────────────────
cloudflare-worker/.wrangler/
cloudflare-worker/.dev.vars
.wrangler/

# ── Python (local dev server only) ───────────
# The venv folder is NOT part of this project.
# Run: python3 -m http.server 8080
# Do NOT commit venv/ — it's hundreds of MB of pip internals.
venv/
__pycache__/
*.pyc
*.pyo

# ── OS files ──────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Editor / IDE ──────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
